export enum TaskStatus {
  Todo = 'TODO',
  InProgress = 'IN_PROGRESS',
  Blocked = 'BLOCKED',
  InQA = 'IN_QA',
  Done = 'DONE',
  Deployed = 'DEPLOYED',
}

export enum TaskLocalStorageKeys {
  Tasks = 'TASKS',
}
